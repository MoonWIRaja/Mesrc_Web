## current focus
- keep project memory aligned with the current MESRC landing page, admin CMS behavior, and repo workflow
- preserve admin-managed content, uploaded assets, and image-framing behavior as part of the repository state

## next
- continue feature or UI fixes only when explicitly requested
- if admin content changes again, commit `apps/web/data/site-data.json` and any new files in `apps/web/public/uploads`
- if landing/admin previews drift again, prefer shared card components or shared presentation helpers instead of duplicate preview markup

## blockers
- none
