## current focus
- keep project documentation and project memory in sync with the current MESRC repo state
- preserve the current admin-managed content and uploads as part of the repository workflow

## next
- continue feature or UI fixes only when explicitly requested
- if admin content changes again, commit `apps/web/data/site-data.json` and any new files in `apps/web/public/uploads`

## blockers
- none
