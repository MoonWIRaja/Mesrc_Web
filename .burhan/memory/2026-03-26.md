## 2026-03-26
- rewrote the root `README.md` to match the actual MESRC project, including install, usage, admin CMS behavior, env vars, commands, data persistence, and deployment notes
- prepared `.burhan/memory` scaffolding and updated it with stable repo facts, user preferences, and current workflow notes

### content and data system
- centralized current MESRC defaults into `apps/web/lib/data/defaults.ts`
- aligned `store.ts`, API fallbacks, and several admin/section fallback states to use the shared defaults instead of stale placeholder content
- confirmed that admin-managed content persists through `apps/web/data/site-data.json`
- confirmed that uploaded assets persist through `apps/web/public/uploads`
- documented that these files must be committed if the latest admin state should move with the repo

### landing page and section updates
- fixed Services carousel/card behavior so cards keep a more consistent height, keep the image-left/text-right layout, and avoid the large loop gap issue
- fixed Doctors carousel/card behavior in the same direction as Services, including adaptive width behavior and better consistency across cards
- updated navbar brand click to return to the hero section via `/#hero` and added a hero anchor id
- removed the unused CEO signature from the CEO section, admin flow, and default data model
- confirmed Gallery social links are controlled by gallery admin URL fields for TikTok, Instagram, and Facebook
- cleaned several leftover fallback labels/placeholders so the default system reflects MESRC instead of starter/sample content

### admin ui and image focus system
- added shared `Image Focus` controls for hero, about, ceo, doctors, services, and promotions
- standardized the editor layout to preview on the left and settings on the right with horizontal, vertical, zoom, opacity, and blur controls
- made the Image Focus panel use the same general layout style across sections
- upgraded several previews to be closer to the landing page shape instead of generic image boxes
- extracted or reused shared cards where needed so admin preview matches landing more closely, especially for CEO and promotions
- fixed shared image presentation behavior so horizontal and vertical controls visibly pan instead of appearing broken
- adjusted CEO image rendering specifically so the image stays inside the box properly and can be framed more fully during editing

### admin list/card polish
- normalized Doctors and Promotions admin list areas to use full-width list flow like Services instead of narrow multi-column cards
- aligned card/list behavior in admin with the user’s preference for cleaner, service-style content presentation

### repo and verification
- created and pushed the private GitHub repo `MoonWIRaja/Mesrc_Web`
- confirmed repo remote as `origin https://github.com/MoonWIRaja/Mesrc_Web.git`
- standard verification used during this work was `npm exec --workspace apps/web -- tsc --noEmit`

### recent earlier repo work still relevant
- recent git history before the README/memory update already included time display improvements that replaced the previous time picker flow with a read-only time display/modal approach to avoid overflow and improve accessibility
